org 100h
.model small

.data

.code
    mov ax, 10
    mov bx, 20
    mov cx, 30
    
    mov dx, ax
    mov ax, bx
    mov bx, cx
    mov cx, dx
    
    mov dx, ax
    mov ax, bx
    mov bx, cx
    mov cx, dx
    
    mov dx, ax
    mov ax, bx
    mov bx, cx
    mov cx, dx
    
    
    
    
ret    